# HANDOFF — prowebs (May i premium landing page)

## מה זה
דף נחיתה פרימיום לסוכנות "May i" — בניית אתרים/דפי נחיתה/דפי מכירה בלבד (לא כל שירותי הסוכנות). רמת ייחוס: kadabra.co.il, k95.it/en/studio. מיקוד: חברות גדולות, אתרי תדמית פרימיום. **בלי מחירים בשום מקום, בלי מכירה קשה** — "חוויה במקום שכנוע". שורת מותג: "אנחנו לא רודפים - אנחנו נבחרים".

## מיקום + הרצה
תיקייה: `/Users/Rotem/premium websites /prowebs/`
שרת מקומי: `python3 -m http.server 8790 --bind 127.0.0.1` (מהתיקייה)

## מבנה נוכחי (בנוי ועובד)
1. **Loader + Hero** — קלפים עפים למעגל → הופכים לגלריה אופקית נעה (showcase.js). לחיצה על קלף = פופאפ עם האתר החי ב-iframe. לוגו May i מיני 3D למעלה — **נעול, לא לגעת**. CTA "מתחילים מכאן". רקע כוכבים גלובלי (starfield.js, canvas fixed) רציף לאורך כל האתר.
2. **סטורי גלילה (pins)** — בגלילה בהירו: טיל ווקסל-כרום 3D גדל מאחורי הגלריה (מוטה 45° שמאלה-למעלה). סקשן 2 נצמד: הטיל זז שמאלה, שורת מניפסט 1 נחשפת מילה-מילה (מסכות, ימין), הטיל מתחלף ב-3 דמויות אנשים ווקסל (האמצעית גדולה יותר), שורה 2 מתחת לאנשים + האמצעית נצבעת ורוד מותג (#ff007f). הטקסטים יוצאים באותה צורה שנכנסו, הבמה חוזרת למרכז, **האנשים נשארים צפים מאחורי הסקשנים הבאים** (סגנון k95) עד שיוחלפו בהמשך.
3. **וידאו טסטמוניאלס** (בנוי, אושר חלקית — ממתין לאישור סופי שלה) — כותרת עברית קטנה + "May I Help You?" גדול ב-Dancing Script עם shine כל 3 שניות. מרקיזה אוטומטית איטית, ניתנת לעצירה/גרירה/קליק להפעלת וידאו. 4 סרטונים: עמית ברק (מטפל NLP ומאמן אישי), שנינה עזריאל (מדריכת שחייה), חיים עזריאל (עמותת חסדי סעדה), ראובן יוסופוב (סוכן ביטוח). קלפים 9:16 בלי radius, hover = טבעת conic מסתובבת ורודה.

## מה נשאר לבנות (הסדר שסוכם)
4. **תהליך העבודה** — 4 שלבים: אפיון+דדליין משותף → עיצוב ובנייה בשקיפות → השקה → ליווי לשנים.
5. **ביקורות כתובות 5★** (ליד הטופס).
6. **FAQ**.
7. **טופס לידים + שאלת סינון** (content.js כבר מכיל qualificationForm עם שאלת timeline; webhook ל-GoHighLevel עוד לא מחובר).

נדחה לסוף: אודיט טכני מלא (ביצועים/מובייל/SEO/נגישות), reduced-motion לסטורי, og:image, GA4. רעיון פתוח: צבע-מבטא לכל סקשן (כולל גוון לכוכבים) — "נחשוב על זה תכף".

## בידול (הודגש בקופי, בלי מחירים)
זמינות אמיתית, סיעורי מוחות משותפים, שותפות לטווח ארוך, עמידה בדדליינים, שקיפות מלאה.

## קבצים
- `index.html` — כל הסקשנים + סקריפט inline: Lenis+GSAP ScrollTrigger bridge, טיימליין הסטורי, מנוע המרקיזה.
- `js/voxel-logo.js` — מנוע הווקסל (CFG נעול: res 48, gap 0.34, seed 12345, כרום metalness 1 roughness 0.04). `mountVoxelLogo` (full/mini/still) + `mountVoxelPeople` (קנבס אחד, 3 דמויות, midMaterial חשוף לצביעת ורוד).
- `js/showcase.js` — קלפים/גלריה/פופאפ. מפת LIVE של אתרי הדגמה.
- `js/starfield.js` — רקע חלל גלובלי + warp לעכבר.
- `css/style.css`, `css/showcase.css`, `css/voxel.css`, `css/sections.css` (טסטמוניאלס).
- `content.js` — קופי + qualificationForm.
- `assets/testimonials/` — mp4+webp. `assets/cards/` — תמונות קלפים.

## סגנון
Anka (כותרות 800) + Rubik (גוף), ורוד מותג #ff007f, רקע חלל כהה, RTL עברית, Dancing Script לאנגלית דקורטיבית. פונטים self-hosted.

## חוקים קריטיים לצ'אט הבא
- **חיסכון בטוקנים אגרסיבי**: בלי סקרינשוטים אלא אם אין ברירה (ואז אחד, חתוך). בדיקות DOM טקסטואליות בלבד. smoke test מלא רק בסוף באצ'. תשובות קצרות.
- לוגו May i (המיני בהירו + התנהגותו) — נעול.
- בלי מחירים/רמזים למחיר בשום מקום.
- בדיקת גלילה ב-Playwright: רק `page.mouse.wheel` (scrollTo נלחם ב-Lenis).
- `scratchpad/smoke_test.py` קיים — להשתמש במשורה.
